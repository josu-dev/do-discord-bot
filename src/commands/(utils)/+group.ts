import { GroupSetupDefinition } from '../+type';


export const config = {
    description: {
        "es-ES": `Comandos variados sin relacion entre si.`,
        "en-US": `Miscellaneous commands with no relation between them.`
    }
} satisfies GroupSetupDefinition;
